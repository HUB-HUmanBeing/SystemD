<div class="modal-body">
    {{ form_start(form, {'attr': {
        'class': 'form-horizontal',
        'action' : path('decline_invitation' ,{
            'project_id' : project_id ,
            'user_id' : user_id
        }
        ) }
    })
    }}
    <textarea name="reply" id=""  rows="4" class="form-control"></textarea>
    {{ form_rest(form) }}
    <input type="submit" value="Décliner l'invitation" class="btn btn-danger" />
    {{ form_end(form) }}
</div>