<div class="modal-body">
    {{ form_start(form, {'attr': {
                    'class': 'form-horizontal',
                    'action' : path('delete_invitation' ,{'invitation_id' : invitation_id}) }
            })
    }}
    {{ form_rest(form) }}
    <input type="submit" value="Annuler l'invitation" class="btn btn-danger" />
    {{ form_end(form) }}
</div>