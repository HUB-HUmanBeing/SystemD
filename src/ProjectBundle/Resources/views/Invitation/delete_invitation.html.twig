{{ form_start(form, {'attr': {
        'class': 'form-horizontal',
        'action' : path('delete_invitation' ,
                            {'project_id' : project_id ,
                                'user_id' : user_id}
             ) }
        })
}}
{{ form_rest(form) }}
<input type="submit" value="Annuler l'invitation" class="btn btn-danger" />
{{ form_end(form) }}